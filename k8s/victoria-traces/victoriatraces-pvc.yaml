apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: victoria-traces-data
  namespace: default
spec:
  storageClassName: manual
  volumeName: victoria-traces-pv
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
